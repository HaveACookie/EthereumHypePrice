spark.sql（“SELECT AVG（market_cap） from df”）.show（）
spark.sql（“SELECT count（market_cap） from df”）.show（）

spark.sql（“SELECT max（market_cap） from df”）.show（）

spark.sql（“SELECT min（market_cap） from df”）.show（）